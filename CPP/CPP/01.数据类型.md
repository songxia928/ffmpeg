



# 02.数据类型


## 1 字符串 char
### 1.1 复制（strcpy）、拼接（strcat）
```cpp
char img_name[50];
strcpy(img_name,"/");
strcat(img_name,recRealID2.data());
```

## 2 字符串 string
参考网址：http://www.cnblogs.com/xFreedom/archive/2011/05/16/2048037.html
### 2.1 复制、拼接（.append）
```cpp
string str1="I like C++";
string str2=",I like the world.";
string str3="Hello";
string str4("Hi");

str1.append(str2);
str3.append(str2,11,7);//str2的11个字符开始的7个字符复制到str3后面
str4.append(5, '.');		// 复制5个'.'到str4后面

cout<<str1<<endl;
cout<<str3<<endl;
cout<<str4<<endl;
system("pause");
```

运行结果为:
```cpp
I like C++,I like the world.
Hello World.
Hi.....
```

### 2.2 截取（.substr）
```cpp
position2 = faceTXT[j].find(".");
childImgPath2 = faceTXT[j].substr(0,position2);

string s("12345asdf");
string a = s.substr(0, 5); //从第0位开始的长度为5的字符串
```

（3）查找（.find .find_first_of .find_last_of()）
```cpp
string PathImg	 = "D:\\cs\\Code/111111.jpg";
int position1 = PathImg.find("/");
string name = PathImg.substr(position1+1, -1);
int position2 = name.find_last_of(".jpg");
name = name.substr(0, position2);
```


（4）修改（.replace）
```cpp
int position = Path.find("_");
Path.replace(position, 1, "/");
```

（5）插入（）
```cpp
string exten2  = "*.jpg";
```

（6）比较（）
```cpp
string exten2  = "*.jpg";
```

（7）转char[]（）
```cpp
string pp = "dagah";
char p[8];
int i;
for( i=0;i<pp.length();i++)
	p[i] = pp[i];
p[i] = '\0';
printf("%s\n",p);
cout<<p;

// 长度
int len_line = line.length();

char resultTxtP[200] ;
strcat(resultTxtP, txt_name.c_str()); // 这个不行？
```


（8）删除（.erase）
```cpp
#include <iostream>
#include <string>
using namespace std;
int main()
{
	string str("This is an example phrase.");
	string::iterator it;
	// 第(1)种用法
	str.erase(10, 8);
	cout << str << endl;        // "This is an phrase."
	// 第(2)种用法
	it = str.begin() + 9;
	str.erase(it);
	cout << str << endl;        // "This is a phrase."
	// 第(3)种用法
	str.erase(str.begin() + 5, str.end() - 7);
	cout << str << endl;        // "This phrase."
	return 0;
}


/*
（1）erase(pos,n); 删除从pos开始的n个字符，比如erase(0,1)就是删除第一个字符
（2）erase(position);删除position处的一个字符(position是个string类型的迭代器)
（3）erase(first,last);删除从first到last之间的字符（first和last都是迭代器）
*/


```



2.3整数 int
（1）取整

<1> floor
```cpp
floor of 2.3 is 2.0
floor of 2.6 is 2.0
floor of -2.3 is -3.0
floor of -2.6 is -3.0
```


（2）转换成字符串
```cpp
// int 转 string
int n = 123;
stringstream ss;
string str;
ss<<n;
ss>>str;

// string 转 int
str = "456";
n = atoi(str.c_str());
```

（3）随机数
```cpp
#include <stdlib.h>
#define random(x) (rand()%x)  // 产生0到x-1的随机数
int x_rand = random(4);
```


2.4 CvMat

（1）读
```cpp
FILE *f = fopen("1.jpg", "rb");//随便读一个jpg文件
unsigned char *m = (unsigned char*)malloc(1280*800*3);//分配内存足够大，放得下所读取的jpg文件
fread(m, 1280*800*3, 1, f);//读取文件

CvMat M4 = cvMat(1280, 780, CV_8UC3, m);//这里矩阵的行列随便定义，但是矩阵行列乘积要足够大，要能装下解压后的图片文件，jpg文件解压后会是原文件的N多倍，建议使用jpg文件的宽高作为矩阵的行列。最好事先知道所读取得图片分辨率，不然矩阵太小了，解压会失败。
//namedWindow("Mat_Window", CV_WINDOW_AUTOSIZE);
//imshow("Mat_Window", &M4);
//waitKey(0);
//destroyWindow("Mat_Window");
```

（2）CvMat--》IplImage
```cpp
//注意：浅拷贝 - 同样只是创建图像头，而没有复制数据 
IplImage img2 = M2;
IplImage img2_2 = IplImage(M2);
```

（3）CvMat--》Mat



2.5 Mat

```cpp
（1）读

Mat M2 = imread("1.jpg", CV_LOAD_IMAGE_UNCHANGED);
namedWindow("Mat_Window", CV_WINDOW_AUTOSIZE);
imshow("Mat_Window", M2);
waitKey(0);
destroyWindow("Mat_Window");



（2）像素

读
Mat数据结构，操作灰度图像像素点：
int gray_value = (int)image.at<uchar>(i, j);
操作彩色图像像素点：
int color_value = (int)image.at<Vec3b>(i, j)[k];
说明：
其中gray_value中存放灰度值，image是读入的图像，i表示行，j表示列；
color_value中存放彩色像素值，image是读入的图像，i表示行，j表示列，k表示通道，即R、G、B，取值范围为2、1、0.



Mat grayim(400, 600, CV_8UC1);
//遍历所有像素,并设置像素值
for( int i = 0; i < grayim.rows; ++i)
for( int j = 0; j < grayim.cols; ++j )
grayim.at<uchar>(i,j) = (i+j)%255;


宽高
int ww = grayImage.cols;	// cols列数
int hh = grayImage.rows;  // rows 行数

清空
mFaceRects.clear();
current_shape.pop_back(1); // 删除的1行


判空
current_shape.empty();

新建
cv::Mat bias = cv::Mat::ones(1, 1, CV_32FC1);
```


（2）Mat--》IplImage
```cpp
//注意：浅拷贝 - 同样只是创建图像头，而没有复制数据 
IplImage img2 = M2;
IplImage img2_2 = IplImage(M2);
```

（3）Mat--》CvMat
```cpp
//注意：浅拷贝  
Mat a4(3, 3, CV_8UC1, Scalar(1));
CvMat b4 = a4; //a "copy" to b  
	
//注意：深拷贝  
Mat a4_2(3, 3, CV_8UC1, Scalar(255));
CvMat *b4_2 = cvCreateMat(3, 3, CV_8UC1);
CvMat temp4 = a4_2; //转化为CvMat类型，而不是复制数据  
cvCopy(&temp4, b4_2); //真正复制数据  
```

2.6 IplImage

```cpp
（1）读
Mat Img = imread( imgnames[j].c_str(), 0 );	


（2）IplImage--》Mat
//使用Mat的构造函数：Mat::Mat(const IplImage* img, bool copyData=false); 默认情况下copyData为false  
IplImage* srcImg = cvLoadImage("1.jpg");   // srcImg->imageData 表示srcImg的数据首地址
//注意：以下三种效果一致，均为浅拷贝  
Mat M1(srcImg);
Mat M1_1(srcImg, false);
Mat M1_2 = srcImg;
	
//注意：当将参数copyData设为true后，则为深拷贝（复制整个图像数据）  
Mat M1_3(srcImg, true);

（3）IplImage--》CvMat
//法一：cvGetMat函数  
IplImage* img3 = cvLoadImage("1.jpg");   // srcImg->imageData   表示srcImg的数据首地址
CvMat temp3;
CvMat* mat3 = cvGetMat(img3, &temp3);  //深拷贝  

//法二：cvConvert函数  
CvMat *mat3_2 = cvCreateMat(img3->height, img3->width, CV_64FC3);  //注意height和width的顺序  
cvConvert(img3, mat3_2);    //深拷贝  
```

2.7 vector<Rect>、vector<Mat>
```cpp
#include <vector>
vector<string> P_str(14);
for (int i = 0; i < 14; i++)
{
	len_line = line.length();
	line = line.substr(position + 1, len_line - position);
	position = line.find(" ");
	P_str[i] = line.substr(0, position);
}


（1）大小
vector<int> a(10, 0);      //大小为10初值为0的向量a
//对其中部分元素进行输入
cin >> a[2];
cin >> a[5];
cin >> a[6];
//全部输出
int i;
for (i = 0; i<a.size(); i++)
	cout << a[i] << " ";
```



2.8 Scalar()
（1）xx
xxx


2.9CvSize()


CvSize

2.10Ptr<>  智能指针

Ptr<CvFeatureParams> featureParams;




