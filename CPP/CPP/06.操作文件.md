
- [06.文件](#06文件)
  - [1 文件夹](#1-文件夹)
    - [1.1 相对路径（.）](#11-相对路径)
    - [1.2 查看文件夹个数（dir.GetListFolders）](#12-查看文件夹个数dirgetlistfolders)
    - [1.3 新建文件夹（\_mkdir）](#13-新建文件夹_mkdir)
    - [1.4 取文件名（rfind、substr）](#14-取文件名rfindsubstr)
    - [1.5 判断文件是否存在（.open）](#15-判断文件是否存在open)
  - [2 文档](#2-文档)
    - [2.1 txt](#21-txt)
  - [3 图片](#3-图片)
    - [3.1 jpg、bmp](#31-jpgbmp)
      - [（1）查看图片个数（dir）](#1查看图片个数dir)
      - [（2）读取（cvLoadImage、VideoCapture）、显示（cvShowImage）](#2读取cvloadimagevideocapture显示cvshowimage)
      - [（3）取图片名（rfind、substr）](#3取图片名rfindsubstr)
      - [（4）保存（cvSaveImage）](#4保存cvsaveimage)
      - [（5）删除图片（）](#5删除图片)
      - [（6）变换尺寸（cvResize）](#6变换尺寸cvresize)
      - [（7）读取像素点](#7读取像素点)
      - [（8）复制图片](#8复制图片)
      - [（9）画框](#9画框)
      - [（10）截取](#10截取)
      - [（11）移动](#11移动)
      - [（12）旋转](#12旋转)
  - [4 视频](#4-视频)
    - [4.1 MP4](#41-mp4)
  - [5 other](#5-other)
    - [5.1 bin](#51-bin)




# 06.文件


## 1 文件夹
### 1.1 相对路径（.）
```cpp
当前路径 E:\test\file
./   	表示当前路径，相当于E:\test\file
../  		表示当前路径的上一级路径，相当于E:\test
../../  	表示当前路径上上一级路径，相当于E:

“\”:转义字符

string reconizePath = ".\\Test_Img2\\Glasses";
```

### 1.2 查看文件夹个数（dir.GetListFolders）
```cpp
cv::Directory  dir;
string exten1  = "*";
bool   addpath = true;
vector<string> files1 = dir.GetListFolders(Path,exten1,addpath);
int n_files1 = files1.size();
```


### 1.3 新建文件夹（_mkdir）
```cpp
#include <direct.h>
if( _mkdir( char1_file1 ) == 0 )	printf( "%d file1 Success \n",i );
else								printf( "%d file1 Fail \n",i );


if (_access(saP, 0) == -1)
{
	_mkdir(saP);
}
```


Linux
```cpp
#include <sys/types.h>  
#include <sys/stat.h>  
#include <dirent.h>  
if(mkdir(savePath.c_str(), 0755) < 0){
		//printf("can't mkdir \n");
		//return -1;
}
```


### 1.4 取文件名（rfind、substr）
```cpp
string::size_type position1,position2;
string recRealID;
Position1 = foldernames[i].rfind("/");
Position2 = foldernames[i].rfind("-");
recRealID= foldernames[i].substr(position1+1,position2-position1-1);
```


### 1.5 判断文件是否存在（.open）
```cpp
// 判断bin文件是否存在
fstream _file;
_file.open(Path_Img, ios::in);
if (!_file)
{
	cout << Path_Img << "没有被创建";
}
else
{
	cout << Path_Img << "已经存在";
}
file.close();


if (_access(saP, 0) == -1)
{
	_mkdir(saP);
}
```

Linux
```cpp
#include <stdio.h>
#include <unistd.h>
#include <fcntl.h>
int file_Acc;
if((file_Acc = access (imgPath2.c_str(),F_OK)) == 0)
{
	cnt_haveImg++;
	//printf("[access] file exist!/n");
}
else 
{
	cnt_noImg++;
	//printf("[access] file not exist!!/n");
	continue;
}
```


## 2 文档

### 2.1 txt
```cpp
// ============ （1）读
ifstream fin("./TXT/test.txt");    
string line;
while(getline(fin,line)){	//逐行读取fin中的数据保存在line中
    cout << line << endl;
}
fin.close();

// ============ （2）写
#include <fstream>
using namespace std;
ofstream fout("./Report/output.txt"); 			// 打开、新建txt

fout << "   Glass总数";  						// 写字符串
fout <<setw(12)<<right<<s_3_NoGlass<<"\n";  	// 12位，右对齐，写整数
fout <<setw(9)<<right<<setiosflags(ios::fixed)<<setprecision(2) <<p_ok <<"%";		// 9位，右对齐，保留两位小数，写小数

fout << flush; 			// 将缓存中的数据写入txt
fout.close();
```


## 3 图片

### 3.1 jpg、bmp
#### （1）查看图片个数（dir）
```cpp
string exten2  = "*.jpg";
bool   addpath = true;
Directory dir;
vector<string> imagenames =dir.GetListFiles(Path2,exten2,addpath);
```

#### （2）读取（cvLoadImage、VideoCapture）、显示（cvShowImage）
```cpp
IplImage* crop_img = cvCreateImage(cvSize(128, 128),8,1);
cvZero(crop_img);
for (int h = 0; h < imagenames.size(); h=h+1)
{
crop_img = cvLoadImage(imagenames[h].c_str(),-1);
			//cvLoadImage( filename, -1 ); 默认读取图像的原通道数
			//cvLoadImage( filename, 0 ); 强制转化读取图像为灰度图
			//cvLoadImage( filename, 1 ); 读取彩色图
	cvNamedWindow("crop",CV_WINDOW_AUTOSIZE);
	cvShowImage("crop",crop_img);
	cvWaitKey(0);
}
cvReleaseImage(&crop_img);


// Mat读取
Mat Image = imread(Path_img, CV_LOAD_IMAGE_UNCHANGED);
```


#### （3）取图片名（rfind、substr）
```cpp
string::size_type position1;
string recRealID2;
Position1 = folder1names[i].rfind("/");
recRealID2= folder1names[i].substr(position1+1,-1-position1-1);
```


#### （4）保存（cvSaveImage）
```cpp
char Path1_save[200] ="D:/cs/Pic/pose/";;
strcat(Path1_save,iecRealID2.data());	
cvSaveImage(Path1_save, crop_img);			// 保存


Mat img(1000, 1000, CV_8UC3, Scaler(0, 255, 0));
IplImage qImg;
qImg = IplImage(img); // cv::Mat -> IplImage 
cvSaveImage("./out.jpg", &qImg);
```



#### （5）删除图片（）
```cpp
int result = remove(fileT[ft].c_str());  //绝对或者相对路径都可以
```


#### （6）变换尺寸（cvResize）
```cpp
// ---- IplImage ---- 
IplImage* resizeImg = cvCreateImage(cvSize(64,64),8,1);
cvZero(resizeImg);
cvResize(crop_img,resizeImg);
			//• CV_INTER_NN - 最近邻差值, 
			//• CV_INTER_LINEAR -  双线性差值 (缺省使用) 
			//• CV_INTER_AREA -  使用象素关系重采样。当图像缩小时候，该方法可以避免波纹出现。当图像放大时，类似于  CV_INTER_NN  方法.. 
			//• CV_INTER_CUBIC -  立方差值.

// ---- Mat ---- 
Mat Img = imread( imgnames[j].c_str(), 0 );	
Mat resizeImg;
resize(Img,resizeImg,Size(128,128),0,0,CV_INTER_LINEAR);
resize(frame, frame2, Size(frame.cols*0.5, frame.rows*0.5), 0, 0, CV_INTER_LINEAR); // resize
```


#### （7）读取像素点
```cpp
uchar *ptrSrc=(uchar *)(src->imageData); //指针指向图片数据首地址
for (int m = 0; m < src->height; m++)
{
	for (int n = 0; n < src->width; n++)
	{
		uchar val = ptrSrc[m * src->widthStep + n];
	}
}
```

#### （8）复制图片
```cpp
string PImg	 = "D:\\cs\\Code\\LFW_Crop/1.jpg";
string PSImg	 = "D:\\cs\\Code\\LFW_Crop2/2.jpg";

WCHAR buf1[500];
memset(buf1, 0, sizeof(buf1));
MultiByteToWideChar(CP_ACP, 0, PImg.c_str(), strlen(PImg.c_str()) + 1, buf1, sizeof(buf1) / sizeof(buf1[0]));
WCHAR buf2[500];
memset(buf2, 0, sizeof(buf2));
MultiByteToWideChar(CP_ACP, 0, PSImg.c_str(), strlen(PSImg.c_str()) + 1, buf2, sizeof(buf2) / sizeof(buf2[0]));
CopyFile(buf1, buf2, TRUE);
```

Linux
```cpp
#include <stdio.h>
#include <iostream>
int CopyFile(const char *SourceFile,const char *NewFile)
{
    ifstream in;
    ofstream out;
    in.open(SourceFile,ios::binary);//打开源文件
    if(in.fail())//打开源文件失败
    {
        cout<<"Error 1: Fail to open the source file."<<endl;
        in.close();
        out.close();
        return 0;
    }
    out.open(NewFile,ios::binary);//创建目标文件 
    if(out.fail())//创建文件失败
    {
        cout<<"Error 2: Fail to create the new file."<<endl;
        out.close();
        in.close();
        return 0;
    }
    else//复制文件
    {
        out<<in.rdbuf();
        out.close();
        in.close();
        return 1;
    }
}
```


#### （9）画框
```cpp
IplImage* img
for (int f=0;f<23;f++)
{
cvRectangle(img,cvPoint(fP.c[f]-1,fP.r[f]-1),cvPoint(fP.c[f]+1,fP.r[f]+1),cvScalar(255,255,255,0));
}

cvShowImage("img",img);
cvWaitKey(0);
```

#### （10）截取
```cpp
CvSize size = cvSize(40, 50);//区域大小
CvRect box = cvRect(10, 10, size.width, size.height);
cvSetImageROI(img, box);//设置源图像ROI
IplImage* pDest = cvCreateImage(size, img->depth, img->nChannels);//创建目标图像
cvCopy(img, pDest); //复制图像
cvSaveImage("Roi.jpg", pDest);//保存目标图像
cvResetImageROI(img);//源图像用完后，清空ROI


Mat image= imreadimag）；
Rect rect(10, 20, 100, 50); // Rect(x, y, width, height);
Mat image_roi = image(rect);
```


#### （11）移动
```cpp
string Path_Img      = "D:\\1\\1.jpg";
string Path_Img_Save = "D:\\2\\1.jpg";

MoveFile(Path_Img.c_str(), Path_Img_Save.c_str());


string PImg	 = "D:\\cs\\Code\\LFW_Crop/1.jpg";
string PSImg	 = "D:\\cs\\Code\\LFW_Crop2/2.jpg";

WCHAR buf1[500];
memset(buf1, 0, sizeof(buf1));
MultiByteToWideChar(CP_ACP, 0, PImg.c_str(), strlen(PImg.c_str()) + 1, buf1, sizeof(buf1) / sizeof(buf1[0]));
WCHAR buf2[500];
memset(buf2, 0, sizeof(buf2));
MultiByteToWideChar(CP_ACP, 0, PSImg.c_str(), strlen(PSImg.c_str()) + 1, buf2, sizeof(buf2) / sizeof(buf2[0]));
MoveFile(buf1, buf2, TRUE);
```

Linux
```cpp
#include <stdio.h>
#include <iostream>

if(rename(Path_src.c_str(), Path_dst.c_str()) < 0)// 移动
{
	cout<< "Move file error !"<<endl;
}
```

#### （12）旋转
```cpp
CvSize size = cvSize(40, 50);//区域大小
CvRect box = cvRect(10, 10, size.width, size.


//逆时针旋转图像degree角度（原尺寸）  
void rotateImage(IplImage* img, IplImage *img_rotate, int degree)
{
	//旋转中心为图像中心  
	CvPoint2D32f center;
	center.x = float (img->width / 2.0 + 0.5);
	center.y = float (img->height / 2.0 + 0.5);
	//计算二维旋转的仿射变换矩阵  
	float m[6];
	CvMat M  = cvMat(2, 3, CV_32F, m );
	cv2DRotationMatrix(center, degree, 1, &M);
	//变换图像，并用黑色填充其余值
	cvWarpAffine(img, img_rotate, &M, CV_INTER_LINEAR + CV_WARP_FILL_OUTLIERS, cvScalarAll(0));
}


// Mat 旋转: https://blog.csdn.net/hujiameihuxu/article/details/78810100

```






## 4 视频
### 4.1 MP4
```cpp
// ============== （1）读

string filename = "1.avi";//打开的视频文件
VideoCapture capture;
capture.open(filename);

double rate = capture.get(CV_CAP_PROP_FPS);//获取视频文件的帧率
int delay = cvRound(1000.000 / rate);

if (!capture.isOpened())//判断是否打开视频文件
{
	return -1;
}

else
{
	while (true)
	{
		Mat frame;
		capture >> frame;//读出每一帧的图像
		if (frame.empty()) break;
		imshow("处理前视频", frame);
		processiamge(frame);
		imshow("处理后视频", frame);
		waitKey(delay);
	}
}

// ============== 摄像头
VideoCapture capture(0);//如果是笔记本，0打开的是自带的摄像头，1 打开外接的相机
double rate = 25.0;//视频的帧率
Size videoSize(1280, 960);
VideoWriter writer("VideoTest.avi", CV_FOURCC('M', 'J', 'P', 'G'), rate, videoSize);
Mat frame;

while (capture.isOpened())
{
	capture >> frame;
	writer << frame;
	imshow("video", frame);
	if (waitKey(20) == 27)//27是键盘摁下esc时，计算机接收到的ascii码值
	{
		break;
	}
}

// ============== （2）写

#include <stdio.h>
#include <opencv2/core/core.hpp>
#include <opencv2/highgui/highgui.hpp>
#include <opencv2/imgproc/imgproc.hpp>
using namespace std;
using namespace cv;

int main(){
	VideoCapture cap(0);
	if (!cap.isOpened()){
		printf("open video failed!\n");
		return 1;
	}

	Mat Frame;	
	cv::Size sWH = cv::Size((int)cap.get(CV_CAP_PROP_FRAME_WIDTH),
		(int)cap.get(CV_CAP_PROP_FRAME_HEIGHT)); 	//获取当前摄像头的视频信息
	double rate = capture.get(CV_CAP_PROP_FPS);		//获取视频文件的帧率

	VideoWriter outputVideo;
	int frameNum = 100;	//设置保存的视频帧数目
string outputVideoPath = "..\\test.avi";	//保存视频的路径
	outputVideo.open(outputVideoPath, CV_FOURCC('M', 'P', '4', '2'), rate, sWH);

	while (cap.isOpened() && frameNum > 0){
		cap >> Frame;
		if (Frame.empty()) break;
		outputVideo << Frame;
		frameNum--;

		imshow("img", Frame);
		waitKey(10);

		if (char(waitKey(1)) == 'q') break;
	}

	outputVideo.release();
	system("pause");
}


CV_FOURCC---编码格式：
CV_FOURCC('P', 'I', 'M', '1') = MPEG-1 codec
CV_FOURCC('M', 'J', 'P', 'G') = motion-jpeg codec
CV_FOURCC('M', 'P', '4', '2') = MPEG-4.2 codec 
CV_FOURCC('D', 'I', 'V', '3') = MPEG-4.3 codec 
CV_FOURCC('D', 'I', 'V', 'X') = MPEG-4 codec 
CV_FOURCC('U', '2', '6', '3') = H263 codec 
CV_FOURCC('I', '2', '6', '3') = H263I codec 
CV_FOURCC('F', 'L', 'V', '1') = FLV1 codec
```


## 5 other

### 5.1 bin

```cpp
// ============== （1）读
float *R1 = (float *)malloc(L * sizeof(float));
FILE *fid = fopen("./dat/R1.bin","rb");
fread(R1, sizeof(float), L, fid);
fclose(fid);

// ============== （2）写
// 新建bin
FILE* fid=fopen("./FeatureBin/GlassFeatureBin.bin","wb");

for (int h = 0; h < imagenames.size(); h=h+1)
{
	// 保存bin
	fwrite(hogFeature,sizeof(float),HOG_FEATURE_DIM,fid);

	// 打印
	printf("Glasses: %d \n",h);
}
fclose(fid);
```



